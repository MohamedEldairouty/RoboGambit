# === Virtual environments ===
venv/
env/
.venv/
ENV/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === IDE / Editor ===
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# === Calibration data (user-specific, regenerated per setup) ===
# Comment out the next line if you want to commit a default calibration
config/sqdict.json

# Arm calibration is hardware-specific. Each robot needs its own.
# But we DO commit the template file so the IK node has something to load
# before calibration runs. The template is just placeholder values.
ros2_ws/src/robogambit_ik/robogambit_ik/arm_config_user.json

# === Logs / temporary ===
*.log
logs/
tmp/
*.tmp

# === ROS build artifacts (when ROS is integrated) ===
build/
devel/
install/
log/
.catkin_workspace
*.bag

# === OS ===
Thumbs.db
ehthumbs.db
